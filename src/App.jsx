// Stage 1 of the Vite migration: the entire dashboard still lives in one
// module (src/vital-dashboard.jsx). Stage 2 will split it into pages/
// components/hooks/lib.
export { default } from './vital-dashboard.jsx';
