// Stub for Peloton data — returns an empty payload until the live integration lands.
const EMPTY_PELO = {total:0,byDisc:{},recent:[],totalOutput:0,totalCals:0,avgHR:0,avgOutput:0,trend:[],allWorkouts:[]};

export function usePelo() { return EMPTY_PELO; }
